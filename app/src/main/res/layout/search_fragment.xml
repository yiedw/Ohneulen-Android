<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/tools">

    <data>

        <variable
            name="fragment"
            type="com.goodchoice.android.ohneulen.ui.search.Search" />

        <variable
            name="viewModel"
            type="com.goodchoice.android.ohneulen.ui.search.SearchViewModel" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/search"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/search_searchCon"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:focusable="true"
            android:focusableInTouchMode="true"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <EditText
                android:id="@+id/search_editText"
                android:layout_width="0dp"
                android:layout_height="40dp"
                android:background="@drawable/background_border_lightgrey"
                android:imeOptions="actionSearch"
                android:inputType="text"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintWidth_default="percent"
                app:layout_constraintWidth_percent="0.8" />

            <ImageView
                android:id="@+id/search_submit"
                android:layout_width="23dp"
                android:layout_height="25dp"
                android:focusable="true"
                android:onClick="@{fragment::searchClick}"
                android:src="@drawable/picture_sample1"
                app:layout_constraintBottom_toBottomOf="@id/search_editText"
                app:layout_constraintRight_toRightOf="@id/search_editText"
                app:layout_constraintTop_toTopOf="@+id/search_editText" />

            <ImageView
                android:id="@+id/search_filter"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:focusable="true"
                android:onClick="@{fragment::filterClick}"

                android:src="@drawable/notifications_icon"
                app:layout_constraintBottom_toBottomOf="@id/search_editText"
                app:layout_constraintLeft_toRightOf="@id/search_editText"
                app:layout_constraintRight_toLeftOf="@id/search_settings"
                app:layout_constraintTop_toTopOf="@id/search_editText" />

            <ImageView
                android:id="@+id/search_settings"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:focusable="true"
                android:src="@drawable/cafe_sample1"
                app:layout_constraintBottom_toBottomOf="@id/search_editText"
                app:layout_constraintLeft_toRightOf="@id/search_filter"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toTopOf="@id/search_editText" />


        </androidx.constraintlayout.widget.ConstraintLayout>

        <FrameLayout
            android:id="@+id/search_map"
            android:layout_width="match_parent"
            android:layout_height="210dp"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toBottomOf="@id/search_searchCon" />

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/search_infoCon"
            android:layout_width="0dp"
            android:layout_height="30dp"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/search_map">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:gravity="center"
                android:text="매장 102"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <TextView
                android:id="@+id/search_switch"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:focusable="true"
                android:gravity="center"
                android:onClick="@{fragment::switchClick}"
                android:text="클릭"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/search_store_rv"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/search_infoCon"
            bind:searchStore="@{viewModel.searchStoreList}"
            bind:searchStoreAdapter="@{viewModel.searchStoreAdapter}" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>